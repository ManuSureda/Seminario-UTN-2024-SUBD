class Transport {
    constructor(transportEmail, licensePlate, routeNumber, password) {
        this.transportEmail = transportEmail;
        this.licensePlate = licensePlate;
        this.routeNumber = routeNumber;
        this.password = password;
    }
}

module.exports = Transport